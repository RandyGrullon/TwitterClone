import React from "react"; 
import Dashboard from "./Dashboard";

const Layout = () => (
  <div className="w-full bg-cover bg-blue-900 bg-center">
    <div className="flex items-center justify-center h-full">
      <Dashboard />
    </div>
  </div>
);

export default Layout;
